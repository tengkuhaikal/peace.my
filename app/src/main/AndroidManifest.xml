<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MADAssignment"
        tools:targetApi="31">
        <activity
            android:name=".education.DetailedActivity"
            android:exported="false" />
        <activity
            android:name=".education.EducationMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Calendar -->
        <activity
            android:name=".calendar.MedicationCalendar"
            android:exported="false" />
        <activity
            android:name=".calendar.CalendarMainActivity"
            android:exported="false" />
        <activity
            android:name=".calendar.AppointmentCalendar"
            android:exported="false" />

        <!-- Appointment -->
        <activity
            android:name=".appointment.AppointmentMainActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".appointment.Radiology"
            android:exported="false" />
        <activity
            android:name=".appointment.Neurology"
            android:exported="false" />
        <activity
            android:name=".appointment.Cardiology"
            android:exported="false" />
        <activity
            android:name=".appointment.Dermatology"
            android:exported="false" />
        <activity
            android:name=".appointment.DateAndTime"
            android:exported="false" />
    </application>

</manifest>